// strict mode is a special mode that we can activate
// in javascript, which makes it easier for us to
// to write a secure javascript code
// To activate strict mode write 'use strict'
// in beginning of script

'use strict';

let hasDriversLicense = false;
const passTest = true;

if(passTest) hasDriverLicense = true;
if (hasDriversLicense) console.log('I can drive :D');